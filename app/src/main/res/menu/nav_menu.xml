<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group
        android:id="@+id/top"
        android:checkableBehavior="none">


    <!-- we can add icons here also to the items -->
    <item android:title="Setup IP Address"
        android:icon="@drawable/ic_ip"
        android:onClick="ipSetup"
        android:id="@+id/ipsetup"/>

    <item android:title="Setup wizard"
        android:icon="@drawable/ic_magic_wand"
        android:id="@+id/setupWizard"
        />
    <item android:title="Settings"
        android:icon="@drawable/ic_settings"
        android:id="@+id/settings"/>

    </group>



    <group android:id="@+id/bottom"
        android:checkableBehavior="none">
    <item android:title="Documentation"
        android:icon="@drawable/ic_documentation"
        android:id="@+id/documentation"
        />
    <item android:title="Credits"
        android:icon="@drawable/ic_coder_head"
        android:id="@+id/credits"
        android:onClick="onClickInfo"/>
    </group>

</menu>